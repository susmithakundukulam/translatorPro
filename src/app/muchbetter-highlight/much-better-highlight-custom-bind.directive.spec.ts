import { MuchBetterHighlightCustomBindDirective } from './much-better-highlight-custom-bind.directive';

describe('MuchBetterHighlightCustomBindDirective', () => {
  it('should create an instance', () => {
    const directive = new MuchBetterHighlightCustomBindDirective();
    expect(directive).toBeTruthy();
  });
});
