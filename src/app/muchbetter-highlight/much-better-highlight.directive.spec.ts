import { MuchBetterHighlightDirective } from './much-better-highlight.directive';

describe('MuchBetterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MuchBetterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
