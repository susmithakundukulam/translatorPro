import { MuchBetterHighlightbindDirective } from './much-better-highlightbind.directive';

describe('MuchBetterHighlightbindDirective', () => {
  it('should create an instance', () => {
    const directive = new MuchBetterHighlightbindDirective();
    expect(directive).toBeTruthy();
  });
});
